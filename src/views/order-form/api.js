import JsonApi from 'devour-client';

const jsonApi = new JsonApi({pluralize: false});

export default {

    /**
     * Updates the cart item quantity
     *
     * @param {tbd} tbd
     *
     * @returns {tbd}
     */
    updateCartItemQuantity() {

    },

    /**
     * Removes a cart item
     *
     * @param {tbd} tbd
     *
     * @returns {tbd}
     */
    removeCartItem() {

    },
}
